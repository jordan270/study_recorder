[project]
name = "study-recorder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Garv", email = "garvkapoor834@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
study-recorder = "study_recorder:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
